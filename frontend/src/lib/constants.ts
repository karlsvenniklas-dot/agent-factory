// Shared layout constants for the timeline + editor shell.
// Imported by the timeline cluster (BUILD TASK 6) and the panels (BUILD TASK 7).

export const TRACK_HEADER_W = 120; // left gutter width for track headers (px)
export const TRACK_H = 64; // height of a single track lane (px)
export const RULER_H = 28; // height of the time ruler (px)
export const MIN_CLIP = 0.05; // minimum clip length on the timeline (seconds)
